import { smartScheduler } from "./app";

// Export for potential future Canva intent registration
export { smartScheduler };

// For now, this is a standalone feature accessible within the app
export function registerSmartSchedulerIntent() {
    console.log("SmartScheduler feature registered");
}

