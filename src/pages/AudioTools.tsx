import * as React from 'react';

// This module has been removed as per user request.
// TTS functionality has been moved to a reusable VoiceoverControl component.
// STT functionality has been removed to simplify the application.
const AudioTools: React.FC = () => {
  return null;
};

export default AudioTools;
