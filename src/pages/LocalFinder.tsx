import * as React from 'react';

// This module has been removed as per user request to consolidate functionality and remove Google Maps Grounding.
const LocalFinder: React.FC = () => {
  return null;
};

export default LocalFinder;
